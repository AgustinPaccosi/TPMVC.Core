@model Sport
@{
    ViewData["Title"] = "Eliminar deporte";
}
<form method="post" asp-controller="Sports" asp-action="Delete">
    <input type="hidden" asp-for="SportId" />
    <h3>Eliminar deporte</h3>
    <hr />
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div>
        <label asp-for="SportName" class="form-control"></label>
        <input asp-for="SportName" disabled type="text" class="form-control" />
        <button class="btn btn-danger">
            Eliminar
        </button>
        <a asp-controller="Brands" asp-action="Index" class="btn btn-success">
            Volver a la lista
        </a>
    </div>
</form>
